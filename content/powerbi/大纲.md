# Power BI Desktop 知识大纲

> 说明：本大纲最多使用四级标题；五级及以下知识点统一用 **Tab 页** 形式汇总在每个四级标题下方，以便罗列所有可能的知识点。

---

## 1. Power BI Desktop 基础与界面

### 1.1 Power BI 概述

#### 1.1.1 Power BI 生态与产品组件

- **Tab：产品组件**：Power BI Desktop、Power BI 服务、Power BI Mobile、Power BI Report Server、Power BI Embedded、Power BI Premium、Microsoft Fabric
- **Tab：工作流**：获取数据 → 数据准备（Power Query）→ 数据建模 → 可视化与报表设计 → 发布共享
- **Tab：版本与更新**：每月功能更新、预览功能开关、64 位/32 位版本、Microsoft Store 版与直接下载版
- **Tab：授权与容量**：免费版、Pro、Premium Per User、Premium 容量、Fabric 容量、Embedded 容量

#### 1.1.2 安装与配置

- **Tab：系统要求**：Windows 版本、内存、CPU、磁盘空间、浏览器兼容性
- **Tab：下载与安装**：Microsoft Store 安装、安装包下载、静默安装、更新方式
- **Tab：登录与账户**：组织账户、Microsoft 账户、多重身份验证、租户登录
- **Tab：全局设置**：区域设置、语言设置、隐私级别、安全设置、数据缓存设置、恢复未保存文件
- **Tab：启动选项**：启动时恢复上次工作、显示开始屏幕、预览功能启用

### 1.2 界面与视图

#### 1.2.1 主界面布局

- **Tab：顶部功能区**：开始、插入、建模、视图、优化、帮助等选项卡
- **Tab：核心窗格**：字段窗格、可视化窗格、筛选器窗格、格式窗格、数据窗格、选择窗格、书签窗格、同步切片器窗格、性能分析器窗格
- **Tab：画布**：报表页画布、网格线、参考线、缩放控制、页面导航
- **Tab：状态栏**：连接模式、查询状态、数据刷新状态

#### 1.2.2 三种视图

- **Tab：报表视图**：可视化设计、页面管理、交互设置
- **Tab：数据视图**：表数据预览、列工具、表工具、数据探查
- **Tab：模型视图**：关系图视图、表关系编辑、属性窗格、显示文件夹

#### 1.2.3 导航与快捷键

- **Tab：常用快捷键**：复制/粘贴、撤销/恢复、切换视图、保存、新建度量值、打开 Power Query
- **Tab：视图切换**：报表视图、数据视图、模型视图、移动布局视图
- **Tab：画布缩放**：Ctrl+鼠标滚轮、适应页面、实际大小、缩放比例
- **Tab：多窗口与重排**：并排窗口、窗格显示/隐藏、重置窗格布局

---

## 2. 数据获取与连接

### 2.1 数据源类型

#### 2.1.1 文件数据源

- **Tab：Excel**：工作簿、工作表、命名范围、表格识别、隐藏/过滤行、多表导入、Excel 数据连接
- **Tab：CSV/文本**：分隔符检测、编码格式、引号处理、换行符处理、区域设置
- **Tab：XML**：XML 文件、XML 架构、嵌套结构解析
- **Tab：JSON**：JSON 文件、嵌套数组、记录展开
- **Tab：PDF**：PDF 表提取、页面选择、OCR 行为
- **Tab：文件夹**：文件夹批量导入、合并文件、文件属性
- **Tab：SharePoint 文件**：SharePoint 文件夹、OneDrive 文件、版本历史

#### 2.1.2 数据库数据源

- **Tab：SQL Server**：服务器/实例、数据库、Windows/SQL 认证、查询语句、高级选项
- **Tab：MySQL/PostgreSQL**：连接字符串、端口、SSL、时区
- **Tab：Oracle**：TNS 名称、服务名、SID、ODAC 驱动
- **Tab：IBM Db2**：数据库名、驱动配置、连接属性
- **Tab：SAP HANA**：服务器、实例号、多租户、变量输入
- **Tab：Azure SQL/Synapse**：服务器地址、数据库、Active Directory 认证、专用 SQL 池
- **Tab：Analysis Services**：实时连接、MDX/DAX 查询、多维/表格模型

#### 2.1.3 云与在线服务

- **Tab：SharePoint Online**：列表、文档库、用户信息
- **Tab：Dynamics 365**：客户参与、财务与运营、Dataverse
- **Tab：Salesforce**：对象、报表、SOQL 查询、自定义字段
- **Tab：Google Analytics**：维度、指标、日期范围、采样设置
- **Tab：Azure 数据服务**：Azure Blob Storage、Azure Data Lake Storage Gen2、Azure Table Storage、Azure Data Explorer
- **Tab：Dataverse**：标准表、自定义表、关系、筛选条件
- **Tab：其他云服务**：Adobe Analytics、Mailchimp、Stripe、Zendesk、ServiceNow 等

#### 2.1.4 其他数据源

- **Tab：Web**：URL 导入、HTML 表、分页数据、需身份验证的 Web 页
- **Tab：OData**：OData 服务 URL、查询选项、版本
- **Tab：REST API**：自定义连接器、Web.Contents、JSON 解析、身份验证头
- **Tab：Python/R 脚本**：Python 数据源、R 数据源、脚本执行环境
- **Tab：Hadoop/Spark**：HDFS、Hive、Spark SQL、HDInsight
- **Tab：ODBC/OLE DB**：DSN 配置、连接字符串、驱动选择
- **Tab：空白查询**：手动输入数据、空白查询创建、M 表达式生成数据

### 2.2 连接方式与参数

#### 2.2.1 连接模式

- **Tab：导入模式**：数据加载到内存、完整 DAX 能力、需刷新
- **Tab：DirectQuery**：实时查询数据源、大数据量场景、限制与不支持功能
- **Tab：双模式**：导入与 DirectQuery 混合、分区级双模式
- **Tab：实时连接**：连接 Analysis Services/Power BI 数据集、无本地数据复制

#### 2.2.2 连接设置

- **Tab：凭据类型**：匿名、Windows、基本、OAuth2、组织账户、服务主体
- **Tab：隐私级别**：无、公共、组织、私有；隐私级别影响合并查询
- **Tab：超时设置**：连接超时、命令超时、重试次数
- **Tab：数据源设置**：编辑凭据、更改源、清除权限、导出数据源
- **Tab：连接加密**：SSL/TLS、证书验证、加密连接选项

#### 2.2.3 参数与动态数据源

- **Tab：创建参数**：新建参数、参数名称、类型、建议值、当前值
- **Tab：参数类型**：文本、数字、日期/时间、列表、任意类型
- **Tab：动态数据源**：用参数替换服务器名、数据库名、文件路径、查询条件
- **Tab：模板文件**：.pbit 模板、参数化模板、发布模板到组织

---

## 3. Power Query 数据准备与转换

### 3.1 Power Query 编辑器基础

#### 3.1.1 查询窗格与数据预览

- **Tab：查询列表**：查询分组、查询搜索、查询依赖关系、查询图标
- **Tab：数据预览**：列标题、行号、数据格式预览、值预览
- **Tab：数据探查**：列质量、列分布、列配置文件、基于前 1000 行的探查
- **Tab：视图选项**：显示查询设置、显示公式栏、等宽字体、预览行数

#### 3.1.2 查询操作

- **Tab：新建查询**：从数据源新建、从现有查询引用、复制查询、合并查询、追加查询
- **Tab：查询管理**：重命名、删除、启用/禁用加载、移动到组、添加说明
- **Tab：查询引用与复制**：引用查询、复制查询、引用与复制的区别
- **Tab：合并查询**：连接类型（左外、右外、内、完全、左反、右反）、模糊匹配
- **Tab：追加查询**：追加两个或多个查询、追加为新查询
- **Tab：分组依据**：基本分组、高级分组、聚合方式

### 3.2 数据清洗与转换

#### 3.2.1 列操作

- **Tab：添加列**：自定义列、条件列、索引列、重复列、示例列
- **Tab：删除列**：删除选中列、删除其他列、删除空列、删除重复列
- **Tab：重命名列**：手动重命名、批量重命名、使用首行作为标题
- **Tab：拆分列**：按分隔符、按字符数、按位置、按大小写转换、按数字/文本拆分
- **Tab：合并列**：合并列、合并列并添加分隔符
- **Tab：透视与逆透视**：透视列、逆透视列、逆透视其他列、仅逆透视选中列
- **Tab：更改数据类型**：文本、整数、小数、日期/时间、逻辑值、货币、百分比、二进制
- **Tab：替换值**：替换值、替换错误、替换空值
- **Tab：填充列**：向下填充、向上填充
- **Tab：移动列**：左移、右移、移动到开头、移动到末尾

#### 3.2.2 行操作

- **Tab：删除行**：删除顶部行、删除底部行、删除间隔行、删除重复项、删除空行、删除错误行
- **Tab：保留行**：保留顶部行、保留底部行、保留行范围、保留重复项、保留错误行
- **Tab：筛选行**：基本筛选、高级筛选、文本筛选、数字筛选、日期筛选
- **Tab：排序**：升序、降序、多列排序
- **Tab：转置**：行转列、列转行
- **Tab：分组统计**：分组依据聚合、多列分组、自定义聚合

#### 3.2.3 文本/数字/日期转换

- **Tab：文本转换**：修剪、清理、大小写转换、添加前缀/后缀、提取文本、长度、位置、替换文本
- **Tab：数字转换**：标准运算、舍入、取整、绝对值、幂、对数、三角函数、统计信息
- **Tab：日期时间转换**：提取年/月/日/小时/分钟/秒、周数、天数、月初/月末、添加天数/月/年、时区转换
- **Tab：条件列**：基于条件生成新列、多条件判断、返回不同值
- **Tab：自定义列**：M 表达式自定义列、复杂逻辑、类型转换

### 3.3 M 语言与高级查询

#### 3.3.1 M 语言基础

- **Tab：语法结构**：`let...in` 结构、步骤名称、表达式、注释（`//` 和 `/* */`）
- **Tab：标识符与大小写**：标识符规则、大小写敏感、特殊字符转义
- **Tab：数据类型**：文本、数字、逻辑、日期、时间、日期时间、时区、持续时间、列表、记录、表、函数、二进制、空值
- **Tab：运算符**：算术、比较、逻辑、文本连接、列表运算

#### 3.3.2 常用 M 函数

- **Tab：列表函数**：List.Sum、List.Count、List.Distinct、List.Transform、List.Select、List.Generate
- **Tab：记录函数**：Record.Field、Record.AddField、Record.TransformFields
- **Tab：表函数**：Table.SelectRows、Table.AddColumn、Table.Group、Table.Join、Table.Combine、Table.NestedJoin
- **Tab：文本函数**：Text.Start、Text.End、Text.Contains、Text.Replace、Text.Split、Text.Combine
- **Tab：数字函数**：Number.Round、Number.Abs、Number.Power、Number.Mod
- **Tab：日期函数**：Date.AddDays、Date.DayOfWeek、Date.StartOfMonth、Date.EndOfYear
- **Tab：时间函数**：Time.Hour、Time.Minute、Time.From
- **Tab：类型函数**：Type.IsNullable、Type.ListItem、Value.Type

#### 3.3.3 高级编辑器与查询折叠

- **Tab：高级编辑器**：查看完整 M 代码、编辑步骤、语法高亮、错误提示
- **Tab：查询折叠概念**：将 M 转换折叠到数据源原生查询、提升性能
- **Tab：查看折叠**：右键步骤查看“查看本机查询”、折叠指示、阻止折叠的因素
- **Tab：阻止折叠因素**：自定义列、复杂 M 函数、数据类型转换、合并查询

### 3.4 数据加载与刷新

#### 3.4.1 加载选项

- **Tab：加载到报表**：将查询结果加载到数据模型
- **Tab：仅连接**：仅创建连接，不加载数据；用于合并查询的中间表
- **Tab：加载到数据模型**：控制哪些表加载、哪些表不加载
- **Tab：从查询编辑器关闭并应用**：应用更改并加载数据

#### 3.4.2 刷新管理

- **Tab：手动刷新**：在 Desktop 中刷新单个表或全部表
- **Tab：后台刷新**：刷新时是否允许后台执行
- **Tab：计划刷新**：发布到服务后的计划刷新设置
- **Tab：增量刷新**：RangeStart/RangeEnd 参数、增量刷新策略、历史数据保留
- **Tab：刷新失败处理**：数据源变更、凭据过期、网关离线、超时错误

---

## 4. 数据建模

### 4.1 数据模型基础

#### 4.1.1 表与关系

- **Tab：事实表与维度表**：事实表特征、维度表特征、粒度、主键、外键
- **Tab：关系基数**：一对多（1:N）、一对一（1:1）、多对多（M:N）
- **Tab：交叉筛选方向**：单向筛选、双向筛选、筛选传递路径
- **Tab：关系的作用**：跨表筛选、RELATED/RELATEDTABLE 函数、数据模型完整性

#### 4.1.2 星型模型与雪花模型

- **Tab：星型架构**：事实表居中、维度表直接关联、最佳实践
- **Tab：雪花架构**：维度表规范化、多层维度、性能影响
- **Tab：模型规范化与反规范化**：减少冗余、提高性能、维度合并
- **Tab：模型架构决策**：何时使用星型、何时使用雪花、扁平淡化

### 4.2 关系管理

#### 4.2.1 创建与编辑关系

- **Tab：模型视图拖拽**：拖拽字段创建关系、自动检测关系
- **Tab：关系对话框**：选择表、选择列、基数、筛选方向、激活状态
- **Tab：编辑关系**：修改现有关系、更改键列、更改基数
- **Tab：自动检测**：自动检测新关系、手动创建关系
- **Tab：删除关系**：删除关系对报表影响

#### 4.2.2 关系高级设置

- **Tab：双向筛选**：开启双向筛选、对性能影响、使用场景
- **Tab：单一/双向安全性**：双向筛选与行级别安全性
- **Tab：活动与非活动关系**：同一表多关系、USERELATIONSHIP 激活
- **Tab：多对多关系**：Power BI 原生多对多、桥接表、多对多限制
- **Tab：关系键选择**：唯一键、组合键、数据完整性检查

### 4.3 模型属性与元数据

#### 4.3.1 表和列属性

- **Tab：表属性**：隐藏、重命名、描述、显示文件夹
- **Tab：列属性**：隐藏、重命名、数据类型、格式、数据类别、排序依据列、描述、默认汇总方式
- **Tab：数据格式**：整数、小数、百分比、货币、日期时间、自定义格式字符串
- **Tab：排序依据列**：按其他列排序、月份名称按月份号排序
- **Tab：数据类别**：地址、城市、国家/地区、纬度、经度、URL、图像 URL、Web URL
- **Tab：默认汇总方式**：无、求和、平均值、最小值、最大值、计数、非重复计数

#### 4.3.2 度量值表与字段组织

- **Tab：创建度量值表**：空表用于存放度量值、命名规范
- **Tab：显示文件夹**：字段窗格中创建文件夹、嵌套文件夹、组织字段
- **Tab：隐藏字段**：隐藏不需要的字段、减少字段窗格杂乱
- **Tab：字段排序**：字段在窗格中的排序、字母排序、自定义顺序
- **Tab：描述与文档**：添加表/列/度量值描述、模型文档化

### 4.4 高级建模

#### 4.4.1 计算列与度量值

- **Tab：计算列定义**：DAX 表达式、行上下文、存储于表中、增加模型大小
- **Tab：度量值定义**：DAX 表达式、动态计算、不占存储、依赖于筛选上下文
- **Tab：计算列与度量值差异**：计算时机、存储、性能、适用场景
- **Tab：度量值属性**：格式、数据类别、描述、默认汇总方式

#### 4.4.2 计算表与计算组

- **Tab：计算表**：使用 DAX 表函数创建表、日期表、辅助表
- **Tab：计算组概念**：计算组用于批量修改度量值、减少度量值数量
- **Tab：计算组创建**：Tabular Editor 创建、Power BI Desktop 原生支持
- **Tab：计算组项**：格式字符串、计算项表达式、优先级
- **Tab：计算组应用场景**：时间智能切换、货币转换、单位切换

#### 4.4.3 父子层次结构

- **Tab：父子层次结构概念**：自引用表、父键、子键
- **Tab：路径函数**：PATH、PATHITEM、PATHLENGTH、PATHCONTAINS
- **Tab：层次结构遍历**：ISINSCOPE、LEVEL、层次结构级别计算
- **Tab：角色扮演维度**：同一维度表多次关联、创建视图

#### 4.4.4 角色与安全性

- **Tab：创建角色**：管理角色、新建角色、角色权限
- **Tab：DAX 筛选器**：行级别安全性表达式、表筛选器
- **Tab：动态安全性**：USERNAME()、USERPRINCIPALNAME()、动态角色
- **Tab：测试角色**：以角色身份查看、验证行级别安全性
- **Tab：对象级安全性**：表/列级安全、元数据安全

---

## 5. DAX（数据分析表达式）

### 5.1 DAX 基础

#### 5.1.1 语法与运算符

- **Tab：公式结构**：度量值名称 = 表达式、计算列 = 表达式
- **Tab：数据类型**：整数、小数、文本、布尔、日期时间、货币、空值
- **Tab：运算符**：算术运算符（+ - * / ^）、比较运算符（= <> > < >= <=）、逻辑运算符（&& || NOT）、文本连接（&）
- **Tab：注释**：`//` 单行注释、`/* */` 多行注释
- **Tab：表构造函数**：`{ }` 创建表、单列表、多列表

#### 5.1.2 上下文

- **Tab：行上下文**：计算列逐行计算、行上下文函数、迭代器
- **Tab：筛选上下文**：切片器、视觉对象、筛选器、CALCULATE 创建
- **Tab：上下文转换**：CALCULATE 将行上下文转换为筛选上下文、EARLIER、迭代器中的上下文转换
- **Tab：上下文优先级**：行上下文与筛选上下文叠加规则

#### 5.1.3 表与列引用

- **Tab：完全限定名**：'表名'[列名]、度量值直接引用
- **Tab：变量**：VAR/RETURN 定义变量、变量作用域、多次使用变量
- **Tab：表函数返回表**：表变量、表表达式、在度量值中使用表
- **Tab：列引用与度量值引用**：列需要聚合函数、度量值直接引用

### 5.2 DAX 函数分类

#### 5.2.1 聚合函数

- **Tab：基础聚合**：SUM、AVERAGE、MIN、MAX、COUNT、COUNTA、COUNTBLANK
- **Tab：计数函数**：COUNTROWS、DISTINCTCOUNT、COUNTX、COUNTAX
- **Tab：迭代器聚合**：SUMX、AVERAGEX、MINX、MAXX、PRODUCTX
- **Tab：统计聚合**：MEDIAN、PERCENTILE、STDEV、VAR、GEOMEAN

#### 5.2.2 筛选函数

- **Tab：CALCULATE**：修改筛选上下文、多个筛选条件、筛选器替换规则
- **Tab：筛选器函数**：FILTER、ALL、ALLEXCEPT、ALLSELECTED、KEEPFILTERS、REMOVEFILTERS、USERELATIONSHIP、CROSSFILTER
- **Tab：信息筛选函数**：ISFILTERED、ISCROSSFILTERED、ISINSCOPE、HASONEFILTER、HASONEVALUE
- **Tab：筛选器值函数**：VALUES、DISTINCT、RELATEDTABLE、CALCULATETABLE

#### 5.2.3 时间智能函数

- **Tab：日期表要求**：连续日期、无重复日期、标记为日期表
- **Tab：累计函数**：TOTALYTD、TOTALQTD、TOTALMTD、DATESYTD、DATESQTD、DATESMTD
- **Tab：同比/环比函数**：SAMEPERIODLASTYEAR、DATEADD、PREVIOUSMONTH、PREVIOUSQUARTER、PREVIOUSYEAR
- **Tab：区间函数**：DATESBETWEEN、DATESINPERIOD、DATESBEFORE、DATESAFTER
- **Tab：移动平均**：MOVINGAVERAGE、滚动窗口计算

#### 5.2.4 逻辑与信息函数

- **Tab：逻辑函数**：IF、SWITCH、AND、OR、NOT、TRUE、FALSE
- **Tab：信息函数**：ISBLANK、ISERROR、ISNUMBER、ISTEXT、ISLOGICAL、ISEMPTY
- **Tab：错误处理**：IFERROR、ISERROR、TRY/CATCH 模式
- **Tab：层次结构信息**：ISINSCOPE、ISFILTERED、ISCROSSFILTERED、HASONEFILTER、HASONEVALUE

#### 5.2.5 文本/数学/日期函数

- **Tab：文本函数**：CONCATENATE、CONCATENATEX、FORMAT、LEFT、RIGHT、MID、LEN、TRIM、UPPER、LOWER、SEARCH、SUBSTITUTE
- **Tab：数学函数**：ROUND、ROUNDUP、ROUNDDOWN、INT、TRUNC、ABS、MOD、POWER、SQRT、LOG、EXP、SIN、COS、TAN
- **Tab：日期函数**：DATE、YEAR、MONTH、DAY、HOUR、MINUTE、SECOND、WEEKDAY、WEEKNUM、EOMONTH、DATEDIFF、TODAY、NOW

#### 5.2.6 关系函数

- **Tab：RELATED**：从维度表获取列值、行上下文使用
- **Tab：RELATEDTABLE**：获取相关表、表表达式
- **Tab：USERELATIONSHIP**：激活非活动关系
- **Tab：CROSSFILTER**：更改筛选方向、计算期间修改关系
- **Tab：ROWNUMBER/INDEX**：在窗口函数中排序

#### 5.2.7 表操作函数

- **Tab：表生成函数**：SUMMARIZE、SUMMARIZECOLUMNS、ADDCOLUMNS、SELECTCOLUMNS、GENERATE、GENERATEALL
- **Tab：表筛选函数**：FILTER、TOPN、DISTINCT、VALUES、UNION、INTERSECT、EXCEPT、ALL、ALLEXCEPT
- **Tab：排名函数**：RANKX、RANK、ROWNUMBER、INDEX
- **Tab：扩展表函数**：CROSSJOIN、NATURALINNERJOIN、NATURALLEFTOUTERJOIN
- **Tab：窗口函数**：WINDOW、OFFSET、ORDERBY、PARTITIONBY

### 5.3 DAX 进阶

#### 5.3.1 计算组与格式字符串

- **Tab：计算组创建**：Tabular Editor 或 Power BI Desktop 创建
- **Tab：格式字符串表达式**：动态格式、自定义格式字符串、计算项格式
- **Tab：计算组应用场景**：时间智能切换、货币转换、单位切换、度量值格式化
- **Tab：优先级**：计算组优先级、计算项优先级、多个计算组叠加

#### 5.3.2 DAX 变量与调试

- **Tab：VAR/RETURN**：变量定义、变量作用域、变量多次使用
- **Tab：变量最佳实践**：提高可读性、减少重复计算、避免复杂嵌套
- **Tab：调试技巧**：使用变量返回中间结果、使用 DAX Studio、使用绩效分析器
- **Tab：常见错误**：循环依赖、上下文错误、类型转换、空值处理

#### 5.3.3 性能优化

- **Tab：避免迭代器滥用**：SUMX/FILTER 组合优化、使用 CALCULATE 替代 FILTER
- **Tab：使用变量**：变量缓存结果、减少重复计算
- **Tab：减少基数**：避免高基数列、减少非重复计数列、优化数据模型
- **Tab：优化 FILTER**：避免行级 FILTER 遍历、使用 CALCULATE 筛选器参数
- **Tab：避免双向筛选**：双向筛选性能影响、使用星型模型
- **Tab：使用 SUMMARIZECOLUMNS**：比 SUMMARIZE 性能更好
- **Tab：DAX Studio 分析**：查询计划、服务器时间、公式引擎与存储引擎

### 5.4 常见 DAX 模式

#### 5.4.1 累计与排名

- **Tab：累计销售额**：TOTALYTD、DATESYTD、CALCULATE + FILTER + ALL
- **Tab：滚动平均**：移动平均、滚动 3/6/12 个月
- **Tab：RANKX 排名**：静态排名、动态排名、并列处理
- **Tab：TOP N**：TOPN 函数、动态 Top N、按度量值筛选

#### 5.4.2 同比/环比/占比

- **Tab：同比（YoY）**：SAMEPERIODLASTYEAR、增长率、差异额
- **Tab：环比（MoM）**：PREVIOUSMONTH、月环比、日环比
- **Tab：占比**：占总计比例、ALL 函数、ALLSELECTED 函数
- **Tab：贡献度**：帕累托贡献、累计占比

#### 5.4.3 动态 ABC 分析

- **Tab：帕累托分析**：累计占比、80/20 规则
- **Tab：ABC 分类**：动态分类、RANKX + 累计占比
- **Tab：动态标题**：使用 DAX 动态生成标题、显示筛选上下文

---

## 6. 可视化与报表设计

### 6.1 可视化基础

#### 6.1.1 可视化类型概览

- **Tab：内置视觉对象**：柱状图、条形图、折线图、面积图、饼图、环形图、散点图、地图、表格、矩阵、卡片、KPI、仪表、漏斗图、瀑布图、树状图、直方图等
- **Tab：自定义视觉对象**：AppSource 视觉对象、组织视觉对象、自定义导入
- **Tab：AI 视觉对象**：关键影响因素、分解树、智能叙述、异常检测、Q&A

#### 6.1.2 添加与配置视觉对象

- **Tab：拖拽字段**：将字段拖到视觉对象、字段井配置
- **Tab：字段井**：轴、图例、值、工具提示、小型序列、列、行、筛选器
- **Tab：格式设置**：视觉对象格式、常规格式、数据标签、标题、背景
- **Tab：视觉对象标头**：视觉对象标头显示、编辑、更多选项

### 6.2 核心可视化对象

#### 6.2.1 基本图表

- **Tab：柱状图/条形图**：堆积、百分比堆积、横向条形图、分组柱状图
- **Tab：折线图/面积图**：折线图、面积图、堆积面积图、百分比堆积面积图
- **Tab：组合图**：柱线组合图、多序列组合、共享轴、次轴
- **Tab：饼图/环形图**：饼图、环形图、百分比标签、排序
- **Tab：散点图/气泡图**：散点图、气泡图、播放轴、分类散点

#### 6.2.2 表格与矩阵

- **Tab：表**：多列显示、行级数据、条件格式、总计
- **Tab：矩阵**：行/列层次、展开/折叠、小计/总计、条件格式
- **Tab：行/列层次**：多级行、多级列、层次结构展开
- **Tab：小计/总计**：行小计、列小计、总计开关、自定义小计

#### 6.2.3 地图与地理

- **Tab：地图**：气泡地图、着色地图、形状地图、ArcGIS 地图
- **Tab：地理编码**：地理数据字段、位置、纬度/经度、数据类别
- **Tab：形状地图**：自定义形状、地区映射、颜色饱和度
- **Tab：ArcGIS 地图**：高级地图分析、人口统计、参考层

#### 6.2.4 其他视觉对象

- **Tab：卡片**：卡片、多行卡片、数字卡片、文本卡片
- **Tab：KPI**：目标、实际、趋势、指标
- **Tab：仪表**：仪表盘、目标值、颜色区域
- **Tab：漏斗图**：阶段转化、排序、标签
- **Tab：瀑布图**：增减变化、起始/结束值、中间步骤
- **Tab：树状图**：层次结构、大小和颜色编码
- **Tab：桑基图**：流向、节点、链接
- **Tab：直方图**：分箱、频率分布、自定义箱宽
- **Tab：箱线图**：四分位数、异常值、统计分布

#### 6.2.5 AI 视觉对象

- **Tab：关键影响因素**：影响度量值的因素、排名、解释
- **Tab：分解树**：多维度分解、路径分析、AI 拆分
- **Tab：智能叙述**：自动生成文本摘要、动态叙述
- **Tab：异常检测**：时间序列异常、自动检测
- **Tab：Q&A**：自然语言查询、同义词设置、Q&A 设置

### 6.3 格式与设计

#### 6.3.1 视觉对象格式

- **Tab：颜色**：主题色、条件色、自定义色、透明度
- **Tab：字体**：字体族、字号、样式、颜色
- **Tab：标题与标签**：视觉对象标题、数据标签、轴标签、图例标题
- **Tab：背景与边框**：背景色、边框、阴影、圆角
- **Tab：工具提示**：默认工具提示、自定义工具提示页、工具提示字段
- **Tab：坐标轴**：X 轴、Y 轴、次轴、刻度、网格线、标签格式
- **Tab：图例**：位置、字体、颜色、图例样式

#### 6.3.2 报表页面设计

- **Tab：页面大小**：16:9、4:3、自定义大小、像素尺寸
- **Tab：页面背景**：背景色、背景图像、透明度
- **Tab：主题**：内置主题、自定义主题 JSON、主题导入/导出
- **Tab：模板**：报表模板、页面模板、布局复用
- **Tab：布局辅助**：网格线、参考线、对齐线、对象对齐与分布

#### 6.3.3 条件格式

- **Tab：字体颜色**：基于字段值、基于规则、色阶
- **Tab：背景色**：规则、色阶、字段值
- **Tab：数据条**：正负值、颜色、方向
- **Tab：图标集**：图标选择、阈值设置
- **Tab：字段值格式**：使用度量值动态格式、格式字符串表达式

### 6.4 交互与钻取

#### 6.4.1 筛选器类型与层次

- **Tab：视觉级筛选器**：只影响当前视觉对象
- **Tab：页面级筛选器**：影响当前页面所有视觉对象
- **Tab：报表级筛选器**：影响整个报表
- **Tab：钻取筛选器**：钻取页面筛选、钻取字段
- **Tab：筛选器面板**：显示/隐藏筛选器、筛选器锁定、筛选器搜索

#### 6.4.2 切片器

- **Tab：切片器类型**：列表切片器、下拉切片器、日期切片器、相对日期切片器、层级切片器、数字切片器
- **Tab：同步切片器**：同步切片器窗格、同步到多个页面、同步可见性
- **Tab：切片器格式**：标题、选择控件、清除按钮、方向、响应式
- **Tab：相对日期切片器**：过去 X 天/月/年、当前期间、动态范围
- **Tab：层级切片器**：多级筛选、级联选择、展开/折叠

#### 6.4.3 钻取

- **Tab：钻取页面**：创建钻取页面、设置钻取字段、钻取按钮
- **Tab：钻取字段**：多字段钻取、钻取筛选器传递
- **Tab：跨报表钻取**：跨报表钻取设置、目标报表、参数传递
- **Tab：按钮钻取**：按钮操作设置钻取、导航到钻取页面

#### 6.4.4 书签与按钮

- **Tab：书签创建**：添加书签、更新书签、删除书签
- **Tab：书签属性**：保存筛选器、切片器、视觉对象状态、显示状态
- **Tab：书签导航**：按钮导航到书签、页面导航、书签组
- **Tab：按钮操作**：返回、前进、钻取、书签、网页 URL、问答
- **Tab：选择窗格**：显示/隐藏视觉对象、图层顺序、分组

### 6.5 报表增强功能

#### 6.5.1 工具提示页

- **Tab：自定义工具提示**：创建工具提示页、设置工具提示字段、工具提示大小
- **Tab：工具提示页设置**：页面信息、允许用作工具提示、页面大小
- **Tab：动态工具提示**：根据悬停字段显示不同工具提示

#### 6.5.2 报表页提示与导航

- **Tab：页面导航按钮**：按钮导航到不同页面、导航标签
- **Tab：重置筛选器按钮**：清除所有筛选器、重置切片器
- **Tab：形状与图像**：插入形状、图像、文本框、链接
- **Tab：页面顺序与隐藏**：页面重命名、隐藏页面、页面导航顺序

#### 6.5.3 移动布局

- **Tab：移动布局视图**：创建移动优化布局、移动画布
- **Tab：移动优化**：调整视觉对象大小、隐藏视觉对象、移动页面
- **Tab：响应式设计**：视觉对象响应式、移动/桌面布局切换
- **Tab：移动报表发布**：发布后移动端查看、移动应用支持

---

## 7. 高级分析与 AI 功能

### 7.1 内置分析功能

#### 7.1.1 趋势线与预测

- **Tab：趋势线**：线性、指数、多项式、对数、幂趋势线
- **Tab：预测**：时间序列预测、预测长度、置信区间、季节性
- **Tab：聚类**：散点图自动聚类、聚类数量、聚类标签
- **Tab：异常检测**：时间序列异常、阈值设置、异常标注

#### 7.1.2 统计摘要与分箱

- **Tab：直方图**：分箱大小、箱数量、频率分布
- **Tab：分箱**：数值分箱、日期分箱、自定义分箱
- **Tab：Top N**：显示前 N 个、基于度量值排序、其他类别
- **Tab：中位数/百分位**：DAX 统计函数、统计摘要视觉对象

### 7.2 自定义视觉对象

#### 7.2.1 获取与导入

- **Tab：AppSource 获取**：从 AppSource 导入自定义视觉对象
- **Tab：文件导入**：导入 .pbiviz 文件、导入自定义视觉对象
- **Tab：组织视觉对象**：管理员部署组织视觉对象、组织商店
- **Tab：更新与删除**：更新自定义视觉对象、删除视觉对象

#### 7.2.2 开发与安全

- **Tab：开发工具**：Power BI Visual Tools、Node.js、pbiviz 打包
- **Tab：视觉对象 API**：数据视图、格式面板、选择管理
- **Tab：隐私与安全**：自定义视觉对象权限、数据访问限制、安全审核
- **Tab：认证与发布**：AppSource 发布、认证流程、组织内发布

### 7.3 Python 与 R 集成

#### 7.3.1 Python/R 脚本视觉对象

- **Tab：启用脚本**：在设置中启用 Python/R 视觉对象
- **Tab：运行环境**：本地 Python/R 安装、包依赖、环境路径
- **Tab：脚本视觉对象**：Python 脚本视觉对象、R 脚本视觉对象、数据传递
- **Tab：可视化库**：matplotlib、ggplot2、plotly、seaborn

#### 7.3.2 Python/R 数据源

- **Tab：Python/R 数据连接**：使用 Python/R 脚本作为数据源
- **Tab：数据转换**：在 Power Query 中使用 Python/R 脚本进行数据清洗
- **Tab：包管理**：安装包、版本兼容、依赖处理

---

## 8. 性能优化与最佳实践

### 8.1 数据加载性能

#### 8.1.1 查询折叠与增量刷新

- **Tab：查询折叠**：将 M 转换折叠到数据源、提升加载速度
- **Tab：增量刷新设置**：RangeStart/RangeEnd、策略、历史数据保留、仅刷新新数据
- **Tab：分区策略**：年/月/日分区、动态分区、混合表
- **Tab：查询诊断**：Power Query 诊断、步骤耗时、查询计划

#### 8.1.2 数据量控制

- **Tab：删除多余列**：只保留需要的列、减少模型大小
- **Tab：筛选必要行**：在源查询中筛选行、减少加载数据量
- **Tab：聚合数据**：在源端聚合、减少明细数据加载
- **Tab：避免高基数**：高基数列、哈希列、减少基数
- **Tab：延迟加载**：仅连接表、按需加载

### 8.2 模型性能

#### 8.2.1 存储与压缩

- **Tab：列存储**：VertiPaq 引擎、列压缩、字典编码
- **Tab：压缩影响因素**：基数、重复值、数据类型、排序
- **Tab：大模型格式**：大型模型存储格式、超过 1GB 模型
- **Tab：内存管理**：模型内存占用、内存限制、卸载表

#### 8.2.2 关系与模型设计

- **Tab：单双向关系**：优先单向关系、双向关系性能影响
- **Tab：星型模型**：避免雪花、维度表扁平化
- **Tab：减少计算列**：用度量值替代计算列、减少模型大小
- **Tab：优化关系**：使用整数键、避免多列关系、减少关系数量
- **Tab：隐藏无关字段**：减少字段窗格杂乱、提升开发效率

### 8.3 DAX 性能

#### 8.3.1 常见性能陷阱

- **Tab：迭代器滥用**：SUMX/FILTER 组合、行级计算
- **Tab：复杂筛选**：FILTER 遍历大表、ALL 函数滥用
- **Tab：大量计算列**：计算列增加模型大小、降低刷新性能
- **Tab：隐式类型转换**：文本与数字混合、转换开销
- **Tab：双向筛选**：双向筛选导致模糊计算

#### 8.3.2 性能分析工具

- **Tab：性能分析器**：视觉对象渲染时间、DAX 查询耗时、排序
- **Tab：DAX Studio**：查询计划、服务器时间、公式引擎与存储引擎
- **Tab：模型分析**：VertiPaq Analyzer、模型大小分析、列压缩分析
- **Tab：Power BI Helper**：模型文档、度量值分析、关系分析

### 8.4 报表渲染性能

#### 8.4.1 视觉对象优化

- **Tab：减少视觉对象数量**：页面视觉对象数量、渲染时间
- **Tab：避免高密度点**：散点图大数据量、采样
- **Tab：优化地图**：地图渲染性能、形状地图替代
- **Tab：图片大小**：图片优化、压缩、延迟加载
- **Tab：避免复杂视觉对象**：自定义视觉对象性能、AI 视觉对象

#### 8.4.2 缓存与设置

- **Tab：查询缓存**：Power BI 服务查询缓存、缓存刷新
- **Tab：持久连接**：DirectQuery 持久连接、连接池
- **Tab：减少书签数量**：书签数量影响性能
- **Tab：筛选器优化**：筛选器面板、同步切片器、减少筛选器

---

## 9. 发布、共享与协作

### 9.1 发布到 Power BI 服务

#### 9.1.1 发布流程

- **Tab：登录账户**：选择发布目标账户、工作区
- **Tab：选择工作区**：我的工作区、共享工作区、发布位置
- **Tab：发布成功/失败处理**：发布错误、覆盖提示、数据集替换
- **Tab：发布选项**：发布报表、数据集、数据流

#### 9.1.2 数据集与报表管理

- **Tab：数据集刷新**：手动刷新、计划刷新、刷新历史
- **Tab：网关配置**：数据网关、数据源映射、凭据设置
- **Tab：报表更新**：重新发布、增量更新、版本管理
- **Tab：数据集认证**：数据集认证、提升可发现性

### 9.2 共享与权限

#### 9.2.1 共享方式

- **Tab：直接共享**：共享报表/仪表板、收件人、权限设置
- **Tab：工作区角色**：管理员、成员、参与者、查看者
- **Tab：应用发布**：创建应用、应用权限、应用发布
- **Tab：嵌入**：网站嵌入、SharePoint 嵌入、Teams 嵌入、Embedded
- **Tab：订阅**：报表订阅、仪表板订阅、邮件订阅

#### 9.2.2 行级别安全性

- **Tab：角色定义**：在 Desktop 中定义角色、DAX 筛选器
- **Tab：成员添加**：在服务中向角色添加用户/组
- **Tab：动态安全性**：使用 USERNAME()/USERPRINCIPALNAME() 实现动态筛选
- **Tab：测试角色**：Desktop 中测试角色、服务中测试角色
- **Tab：对象级安全性**：表/列级安全、元数据安全

### 9.3 协作与开发流程

#### 9.3.1 版本控制与部署

- **Tab：Power BI Desktop 项目文件（.pbip）**：PBIP 格式、拆分文件、源码控制
- **Tab：Git 集成**：Git 仓库、分支管理、版本历史
- **Tab：部署管道**：开发/测试/生产阶段、部署、回滚
- **Tab：ALM Toolkit**：数据集对比、版本迁移、差异分析

#### 9.3.2 数据网关

- **Tab：个人网关**：个人模式、本地数据源、刷新
- **Tab：企业网关**：集群、负载均衡、高可用
- **Tab：数据源管理**：添加数据源、凭据设置、测试连接
- **Tab：故障排查**：网关离线、数据源错误、网络问题

### 9.4 监控与治理

#### 9.4.1 使用指标与审计

- **Tab：使用指标报告**：报表使用情况、用户访问、视图次数
- **Tab：活动日志**：Power BI 活动日志、审计日志
- **Tab：数据分类**：敏感度标签、数据分类
- **Tab：信息保护**：Microsoft Information Protection、加密

#### 9.4.2 管理工具

- **Tab：Power BI Admin Portal**：租户设置、容量管理、用户管理
- **Tab：租户设置**：导出、共享、嵌入、安全设置
- **Tab：Premium 容量管理**：容量分配、负载监控、自动缩放
- **Tab：Fabric 管理**：Fabric 容量、工作区管理、OneLake 管理

---

## 10. 故障排查与常见问题

### 10.1 数据源与刷新问题

#### 10.1.1 连接失败

- **Tab：凭据错误**：用户名/密码错误、OAuth 过期、服务主体凭据
- **Tab：防火墙**：数据库防火墙、IP 白名单、端口阻止
- **Tab：隐私级别**：隐私级别阻止数据源合并
- **Tab：超时**：连接超时、命令超时、网络延迟
- **Tab：驱动缺失**：ODBC/OLE DB 驱动未安装、版本不兼容

#### 10.1.2 刷新失败

- **Tab：网关离线**：数据网关未运行、网关版本过旧
- **Tab：数据源变更**：表结构变化、列重命名、数据类型变化
- **Tab：列类型不匹配**：数据类型转换失败、文本/数字混合
- **Tab：内存不足**：模型过大、容量限制、内存溢出
- **Tab：刷新超时**：刷新时间限制、增量刷新策略

### 10.2 模型与 DAX 问题

#### 10.2.1 计算错误

- **Tab：循环依赖**：计算列循环引用、度量值循环依赖
- **Tab：空白值**：BLANK、ISBLANK、空值传播
- **Tab：上下文错误**：行上下文与筛选上下文混淆、CALCULATE 误用
- **Tab：类型转换**：隐式转换、显式转换、错误值

#### 10.2.2 性能问题

- **Tab：慢查询**：DAX 查询慢、视觉对象加载慢
- **Tab：内存溢出**：模型内存超出限制、刷新内存不足
- **Tab：高 CPU**：复杂 DAX、大量计算、DirectQuery 负载
- **Tab：模型过大**：模型大小超过限制、加载时间过长

### 10.3 视觉对象与报表问题

#### 10.3.1 显示异常

- **Tab：视觉对象不显示**：字段错误、筛选器为空、数据为空
- **Tab：地图空白**：地理数据无法识别、地图视觉对象限制
- **Tab：格式丢失**：主题冲突、格式设置重置
- **Tab：书签错误**：书签状态不一致、视觉对象 ID 变化
- **Tab：自定义视觉对象错误**：视觉对象兼容性、版本问题

#### 10.3.2 交互异常

- **Tab：筛选器未生效**：筛选器方向、关系问题、筛选器面板设置
- **Tab：钻取失败**：钻取页面未设置、钻取字段不匹配
- **Tab：同步切片器异常**：同步设置错误、切片器组不一致
- **Tab：按钮操作失败**：书签不存在、页面隐藏、权限问题

### 10.4 工具与资源

#### 10.4.1 诊断工具

- **Tab：性能分析器**：视觉对象性能、DAX 查询耗时
- **Tab：DAX Studio**：DAX 查询、模型分析、性能跟踪
- **Tab：Power Query 诊断**：查询步骤耗时、数据源请求日志
- **Tab：Fiddler**：网络请求跟踪、API 调试
- **Tab：SQL Profiler**：Analysis Services 跟踪、DAX 事件

#### 10.4.2 社区与文档

- **Tab：官方文档**：Microsoft Learn、Power BI 文档
- **Tab：社区论坛**：Power BI Community、Stack Overflow
- **Tab：博客**：SQLBI、Guy in a Cube、Microsoft Power BI Blog
- **Tab：视频资源**：YouTube 频道、官方培训视频
- **Tab：支持渠道**：Microsoft 支持、Premier 支持、管理员支持

---

## 11. 高级主题与扩展

### 11.1 增量刷新与混合表

#### 11.1.1 增量刷新设置

- **Tab：RangeStart/RangeEnd**：参数创建、数据源筛选
- **Tab：刷新策略**：保留历史数据、增量加载、刷新窗口
- **Tab：历史数据保留**：保留年/月/日、归档数据
- **Tab：增量刷新监控**：刷新历史、数据分区

#### 11.1.2 混合表

- **Tab：导入与 DirectQuery 分区**：混合表概念、分区类型
- **Tab：实时数据**：DirectQuery 分区实时数据、导入分区历史数据
- **Tab：混合表限制**：模型限制、查询限制、刷新限制

### 11.2 计算组与对象级安全性

#### 11.2.1 计算组高级应用

- **Tab：时间智能切换**：计算组切换 YTD/QTD/MTD、度量值复用
- **Tab：度量值格式**：动态格式字符串、格式优先级
- **Tab：计算组优先级**：多个计算组叠加、优先级顺序
- **Tab：计算组与行级别安全性**：计算组在 RLS 下的行为

#### 11.2.2 对象级安全性

- **Tab：表/列级安全**：隐藏表/列、元数据安全
- **Tab：OLS 与 RLS 结合**：对象级安全与行级安全叠加
- **Tab：OLS 实现工具**：Tabular Editor、XMLA 脚本

### 11.3 外部工具与自动化

#### 11.3.1 外部工具集成

- **Tab：Tabular Editor**：模型编辑、计算组、脚本、批量操作
- **Tab：ALM Toolkit**：数据集对比、版本迁移、差异分析
- **Tab：DAX Studio**：DAX 查询、性能分析、模型导出
- **Tab：Power BI Helper**：模型文档、度量值分析、关系分析
- **Tab：外部工具配置**：Power BI Desktop 外部工具集成、自定义工具

#### 11.3.2 自动化与 API

- **Tab：Power BI REST API**：数据集刷新、报表管理、用户管理
- **Tab：PowerShell**：Power BI Cmdlet、自动化脚本
- **Tab：CI/CD**：Azure DevOps、GitHub Actions、自动化部署
- **Tab：嵌入开发**：Power BI Embedded、JavaScript API、嵌入令牌

### 11.4 Power BI Premium 与 Fabric

#### 11.4.1 Premium 功能

- **Tab：XMLA 端点**：XMLA 读写、数据集管理、外部工具连接
- **Tab：高级容量**：Premium 容量、PPU、容量管理
- **Tab：部署管道**：开发/测试/生产、阶段管理
- **Tab：分页报表**：Power BI Report Builder、分页报表发布
- **Tab：AI 功能**：AutoML、认知服务集成

#### 11.4.2 Microsoft Fabric 集成

- **Tab：OneLake**：统一数据湖、数据存储、快捷方式
- **Tab：Direct Lake**：Direct Lake 模式、数据集、性能
- **Tab：Fabric 数据工厂**：数据管道、数据流、数据集成
- **Tab：Fabric 数据工程**：Notebook、Spark、数据科学
- **Tab：Fabric 数据仓库**：Synapse Data Warehouse、Lakehouse

---

## 12. 案例实战与练习

### 12.1 销售分析报表

#### 12.1.1 数据获取与清洗

- **Tab：连接数据源**：SQL Server、Excel、CSV 导入
- **Tab：处理缺失值**：删除空行、填充空值、错误处理
- **Tab：合并维度表**：合并产品表、客户表、区域表
- **Tab：日期表创建**：M 函数或 DAX 创建日期表

#### 12.1.2 建模与 DAX

- **Tab：星型模型**：销售事实表、产品/客户/日期维度表
- **Tab：核心度量值**：销售额、销售量、成本、毛利
- **Tab：时间智能**：YTD、同比、环比、滚动平均
- **Tab：排名与 TOP N**：产品排名、客户 TOP 10

#### 12.1.3 可视化与发布

- **Tab：销售仪表板**：KPI 卡片、趋势图、地图、表格
- **Tab：地图可视化**：地区销售分布、气泡地图
- **Tab：书签与钻取**：产品钻取页、区域钻取页
- **Tab：发布与共享**：发布到服务、设置刷新、共享工作区

### 12.2 财务分析报表

#### 12.2.1 数据源与模型

- **Tab：数据源**：总账、预算、科目维度、日期维度
- **Tab：模型设计**：总账事实表、预算事实表、科目维度、部门维度
- **Tab：关系**：科目维度关联、日期维度关联

#### 12.2.2 度量值与可视化

- **Tab：预算差异**：实际 vs 预算、差异额、差异率
- **Tab：瀑布图**：收入/成本/利润变化、瀑布图展示
- **Tab：KPI**：目标、实际、趋势
- **Tab：矩阵**：科目层次、部门层次、条件格式
- **Tab：动态标题**：显示当前期间、筛选上下文

### 12.3 人力资源分析

#### 12.3.1 数据模型

- **Tab：员工表**：员工信息、部门、职位、入职日期
- **Tab：部门维度**：部门层次、成本中心
- **Tab：离职数据**：离职日期、离职原因
- **Tab：考勤数据**：出勤、请假、加班

#### 12.3.2 度量与报表

- **Tab：在职人数**：期初人数、期末人数、平均人数
- **Tab：离职率**：离职率、新员工流失率、主动/被动离职
- **Tab：年龄分布**：直方图、分箱、分布分析
- **Tab：部门分析**：部门人数、部门离职率、部门对比
- **Tab：趋势分析**：人数趋势、离职趋势、预测

---

## 附录

### 附录 A：常用 DAX 函数速查

- **Tab：聚合类**：SUM、AVERAGE、MIN、MAX、COUNT、COUNTROWS、DISTINCTCOUNT、SUMX、AVERAGEX、MAXX、MINX
- **Tab：筛选类**：CALCULATE、FILTER、ALL、ALLEXCEPT、ALLSELECTED、KEEPFILTERS、REMOVEFILTERS、USERELATIONSHIP、CROSSFILTER
- **Tab：时间智能类**：TOTALYTD、SAMEPERIODLASTYEAR、DATEADD、DATESBETWEEN、PREVIOUSMONTH、DATESYTD
- **Tab：逻辑类**：IF、SWITCH、AND、OR、NOT、IFERROR
- **Tab：信息类**：ISBLANK、ISERROR、ISINSCOPE、ISFILTERED、HASONEVALUE
- **Tab：文本类**：CONCATENATE、CONCATENATEX、FORMAT、LEFT、RIGHT、SEARCH、SUBSTITUTE
- **Tab：表操作类**：SUMMARIZE、SUMMARIZECOLUMNS、ADDCOLUMNS、SELECTCOLUMNS、UNION、INTERSECT、EXCEPT、TOPN、RANKX

### 附录 B：常用 M 函数速查

- **Tab：列表类**：List.Sum、List.Count、List.Distinct、List.Transform、List.Select、List.Generate
- **Tab：记录类**：Record.Field、Record.AddField、Record.TransformFields
- **Tab：表类**：Table.SelectRows、Table.AddColumn、Table.Group、Table.Join、Table.Combine、Table.NestedJoin、Table.Pivot、Table.Unpivot
- **Tab：文本类**：Text.Start、Text.End、Text.Contains、Text.Replace、Text.Split、Text.Combine、Text.Trim
- **Tab：数字类**：Number.Round、Number.Abs、Number.Power、Number.Mod
- **Tab：日期类**：Date.AddDays、Date.DayOfWeek、Date.StartOfMonth、Date.EndOfYear
- **Tab：类型类**：Value.Type、Type.IsNullable、Type.ListItem

### 附录 C：Power BI Desktop 快捷键

- **Tab：常规**：Ctrl+S 保存、Ctrl+Z 撤销、Ctrl+Y 恢复、Ctrl+C 复制、Ctrl+V 粘贴
- **Tab：视图切换**：Ctrl+1 报表视图、Ctrl+2 数据视图、Ctrl+3 模型视图
- **Tab：操作**：Ctrl+N 新建报表、Ctrl+O 打开、Ctrl+P 打印、F5 刷新数据
- **Tab：编辑**：Ctrl+A 全选、Ctrl+D 复制视觉对象、Delete 删除
- **Tab：导航**：Tab 切换字段、Enter 添加字段、Alt+Shift+F10 打开快捷菜单

### 附录 D：术语表

- **Tab：核心术语**：数据模型、关系、基数、筛选上下文、行上下文、度量值、计算列、计算表、计算组
- **Tab：数据准备术语**：Power Query、M 语言、查询折叠、增量刷新、参数
- **Tab：DAX 术语**：上下文转换、迭代器、时间智能、行级别安全性、对象级安全性
- **Tab：服务术语**：工作区、应用、部署管道、数据网关、Premium、Fabric、OneLake
- **Tab：可视化术语**：视觉对象、书签、钻取、工具提示页、同步切片器、条件格式